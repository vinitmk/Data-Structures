package com.mkv.datastructures.linkedlist.singly;

public class PairwiseSwap<T> {

	private void swapPairs(LinkedListNode<T> head) {

	}

	public static void main(String[] args) {
		PairwiseSwap<Integer> main = new PairwiseSwap<>();

		LinkedListMain<Integer> list = new LinkedListMain<>();
		list.createDataStructure(1);
		list.createDataStructure(2);
		list.createDataStructure(3);
		list.createDataStructure(4);
		list.createDataStructure(5);
		list.createDataStructure(6);
	}
}
